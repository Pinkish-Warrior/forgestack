# Shared by both Trivy jobs in secure-pipeline.yml:
# - dependency-scan (scan-type: fs, scan-ref: applications/secure-app)
# - container-scan  (scan-type: image, image-ref: the built secure-app image)
severity:
  - CRITICAL
  - HIGH
exit-code: 1
ignore-unfixed: true
format: table
